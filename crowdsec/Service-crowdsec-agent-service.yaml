apiVersion: v1
kind: Service
metadata:
  labels:
    app: crowdsec
    app.kubernetes.io/managed-by: Helmraiser
  name: crowdsec-agent-service
  namespace: system
spec:
  ports:
  - name: metrics
    port: 6060
    protocol: TCP
    targetPort: 6060
  selector:
    k8s-app: crowdsec
    type: agent
    version: v1
  type: ClusterIP
