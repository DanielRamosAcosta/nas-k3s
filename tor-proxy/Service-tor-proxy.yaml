apiVersion: v1
kind: Service
metadata:
  labels:
    app: tor-proxy
    name: tor-proxy
  name: tor-proxy
  namespace: arr
spec:
  ports:
  - name: tor-proxy-http-tunnel
    port: 8118
    targetPort: 8118
  selector:
    name: tor-proxy
