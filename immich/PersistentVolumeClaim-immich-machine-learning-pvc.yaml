apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: immich
  name: immich-machine-learning-pvc
  namespace: media
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 15Gi
  storageClassName: local-path
  volumeName: immich-machine-learning-pv
