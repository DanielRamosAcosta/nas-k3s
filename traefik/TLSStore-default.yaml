apiVersion: traefik.io/v1alpha1
kind: TLSStore
metadata:
  labels:
    app: traefik
  name: default
  namespace: system
spec:
  defaultGeneratedCert:
    domain:
      main: danielramos.me
      sans:
      - '*.danielramos.me'
    resolver: letsencrypt
